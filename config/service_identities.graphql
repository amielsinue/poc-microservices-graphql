type User @key(fields: "id") {
  id: Int!
  username: String!
  email: String!
}

type Query {
  user(id: Int!): User
  users: [User!]!
}

type Mutation {
  createUser(username: String!, email: String!): User!
}
